/**
 * SMRITI VERCEL SERVERLESS FUNCTION ENTRYPOINT
 * Handles all serverless /api/* invocations by routing through the modular Express application.
 */

import app from '../backend/src/app.js';

export default app;
